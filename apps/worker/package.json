{
  "name": "lp-factory-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 execute lp-factory-db --local --file=./migrations/0001_init.sql",
    "migrate:remote": "wrangler d1 execute lp-factory-db --remote --file=./migrations/0001_init.sql",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250124.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.100.0"
  }
}
