{
  "name": "lp-factory-pixel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 execute ppc-gen-claude --local --file=./migrations/0001_pixel_events.sql",
    "migrate:remote": "wrangler d1 execute ppc-gen-claude --remote --file=./migrations/0001_pixel_events.sql",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250124.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.100.0"
  }
}
